export const COOKIE_BASES = {
    light: 5,
    dark: 5,
};

export const COOKIE_ADDONS = {
    chocolate: 5,
    sprinkles: 3,
    honey: 6,
    cranberries: 4,
    coconut: 10,
};

export type COOKIE_BASES_KEYS = keyof typeof COOKIE_BASES;
export type COOKIE_ADDONS_KEYS = keyof typeof COOKIE_ADDONS;
