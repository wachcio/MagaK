// const calculator = function(number) {
const evenOdd = (number) => {
    if (number === undefined || Number.isNaN(Number(number)) === true)
        return console.log('Wrong input data');
};

const numberOne = prompt('Podaj liczbę');

calculator(numberOne, numberTwo);
